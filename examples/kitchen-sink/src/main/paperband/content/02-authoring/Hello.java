// A source file that a card quotes with {% fragment %}. Only the region between
// the ANCHOR comments is included, so the example stays in step with the code.
public class Hello {
    // ANCHOR: greeting
    public static String greeting(String name) {
        return "Hello, " + name;
    }
    // ANCHOR_END: greeting
}
