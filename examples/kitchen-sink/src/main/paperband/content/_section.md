---
sections: true
---

# Kitchen Sink

This book uses every major Paperband feature once. Each card says which feature it shows
and links to the guide card that explains it. This text is the book's own body: the
content root's `_section.md`, shown on the site's home page and as front matter in the
PDF ([Book Configuration](https://noregressions.github.io/paperband/cards/book-configuration.html)).
`sections: true` in its frontmatter keeps the section grid below it.
