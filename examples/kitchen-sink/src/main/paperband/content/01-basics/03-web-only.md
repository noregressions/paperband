---
id: web-only
oneliner: "On the site only, through a where: predicate in the folder's order."
---

# Site Only

This card is listed in `01-basics/paperband.yaml` with `where: "target == 'web'"`, so it is
on the site and not in the PDF
([Targets](https://noregressions.github.io/paperband/cards/targets.html)).
