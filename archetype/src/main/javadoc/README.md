This module has no Java sources to document — it is a Maven archetype (a
packaged file/resource template), not a library.

This placeholder exists only because Maven Central requires a `-javadoc.jar`
for every non-`pom`-packaged artifact, including `maven-archetype` ones. See
the archetype's own README (and the Paperband guide) for real documentation:
https://github.com/noregressions/paperband
